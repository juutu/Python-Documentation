# Python Coding Portfolio

This repository tracks my progress as I move from programming basics into intermediate Python and automated tools. Instead of just dumping scripts into a folder, I am using this space to break down how my code works under the hood and document the logic behind everything I build.

---

## 📁 Repository Structure

### 🔹 [Basic Python Practice]

This is where I nailed down the fundamentals like basic math calculations, variables, and conditional statements. These early scripts helped me get comfortable with core syntax and program flow without too much friction.

* **Featured Scripts:**
* `automated_delivery_drone.py` (Calculating distance coordinates and optimizing package drop routes)
* `bill_splitter.py` & `movieticketbooking.py` (Handling math logic and user choice routing)
* `bounty_hunter.py` & `caesarcypher.py` (Text manipulation, simple games, and basic encryption logic)
* `employee_profile_generator.py` & `report_card_printer.py` (Storing user variables and printing clean visual layouts)
* `isbn_debug.py` & `medical.py` (Practicing basic algorithmic syntax checks and syntax validations)
* `numpattern.py` & `pinextractor.py` (Manipulating basic matrix layouts and indexing nested text values)
* `user_configurator_manager.py` (Managing settings objects via direct variable modifications)


* **Core Focus:** Processing user inputs, basic string formatting, switching data types, and making clean terminal outputs.

---

### 🔹 [Intermediate Python Practice]

This is where the training wheels came off. The concepts get more abstract here, and I use these scripts to practice "defensive programming"—writing smart code that validates data and stops errors before they cause a crash.

* **Featured Scripts:**
* `budget_app.py` (Constructing class ledgers, cross-category transfer logs, and visual vertical category spending bar charts)
* `game_character_stats_tracker.py` (Implementing secure object-oriented state tracking through properties, forcing attribute variables to stay inside strict thresholds, and overriding string representation formatting)
* `musicalinstrument_inventory.py` (Structuring database blueprints with object-oriented instance tracking parameters)
* `planet_class.py` (Building dynamic system metrics models with custom instance text representations)


* **Core Focus:** Object-Oriented Programming (OOP), explicit data encapsulation, custom properties, and multi-field validation conditions.

---

### 🚀 [Ib Omniscience Portal]

This directory is reserved for my fully realized, end-to-end terminal applications. These milestones demonstrate distinct layered architectures, local JSON state synchronization, and strict execution workflows.

* **Featured Projects:**
* `assignment_planner.py` (A layered task suite that normalizes input strings to lowercase, blocks duplicate trackers, and runs safe dictionary fallback values while updating records locally)
* `formula_bank.py` (An educational reference tool utilizing automatic hardcoded fallback database seeding, case-insensitive query paths, and nested dictionary loops)
* `ib_grade_tracker.py` (An academic assessment engine that parses cumulative grade matrices, evaluates multi-point graduation threshold rules, and handles individual course removals safely)


* **Core Focus:** Persistent data storage, decoupled architecture patterns, input data normalization, and safe JSON serialization.

---

* **[📁 Reflections and Full Breakdowns of Projects]** — Full core review notes, design specs, and systemic architecture summaries.

---

## 🚀 About Me

I am building a strong software engineering foundation by focusing on clean, readable code. Right now, I am working my way through Python, with plans to take this exact structured learning style into SQL and data libraries (Pandas, NumPy, Matplotlib, and Seaborn) in the near future.
